spring:
  config:
    activate:
      on-profile: prod

## Spring data source
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ### will be patched ###
    username: ### will be patched ###
    password: ### will be patched ###

  ## Spring jpa
  jpa:
    properties:
      hibernate:
        ddl-auto: validate
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

## Logging
logging:
  level:
    ododock.webserver: info

app:
  security:
    oauth-callback-uri: "http://localhost:3000/oauth2/callback?sub=%s&provider=%s&access_token=%s&refresh_token=%s}"
